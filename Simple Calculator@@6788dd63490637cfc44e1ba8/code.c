#include<stdio.h>
int main(){
    int a,b;
    char oper;
    scanf("%d%d%c",&a,&b,&oper);
    if(oper==+){
        printf(a+b);
    }
    if(oper==-){
        printf(a-b);
    }
    if(oper==*){
        printf(a*b);
    }
    v
    if(oper== /){
        printf(a/b);
    }
    return 0;
}